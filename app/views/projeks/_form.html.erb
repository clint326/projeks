<%= form_with(model: projek) do |form| %>
  <% if projek.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(projek.errors.count, "error") %> prohibited this projek from being saved:</h2>

      <ul>
        <% projek.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="control">
      <%= form.label :name, style: "display: block", class:"label" %>
      <%= form.text_field :name, class:"input" %>
    </div>
  </div>

  <div class="field">
    <div class="control">
      <%= form.label :status, style: "display: block", class: "label"%>
      <%= form.text_field :status, class: "input" %>
    </div>
  </div>

  <div class="field">
    <div class="control">
      <%= form.label :notes, style: "display: block", class: "label" %>
      <%= form.text_area :notes, rows: "10", class: "textarea" %>
    </div>
  </div>

  <div class="field">
    <div class="control">
      <%= form.submit "Submit", class: "button is-link" %>
    </div>
  </div>
<% end %>
